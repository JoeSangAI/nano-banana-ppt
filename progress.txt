# Ralph Progress Log
Started: 2026年 4月11日 星期六 21时41分46秒 CST
---

## 2026-04-11 21:50 - US-001
- 实现了图片模式枚举和基础数据结构
- 文件变更：创建 utils/image_assets.py
- **Learnings for future iterations:**
  - 项目使用 dataclass 和 Enum 来定义数据结构
  - 图片资产管理采用 Manager 模式，集中管理所有图片资源
  - 代码需要支持序列化（to_dict/from_dict）以便持久化
  - 项目没有配置 mypy，使用 py_compile 和导入测试来验证代码正确性
---

## 2026-04-11 22:00 - US-002
- 实现了轻量读图功能
- 文件变更：修改 utils/image_assets.py
- **Learnings for future iterations:**
  - ImageAssetsManager 需要接收 client 参数才能调用 VLM
  - 轻量读图使用 gemini-2.5-flash 模型，与 image_selector.py 保持一致
  - 图片编码方式：缩放到 512x512，JPEG 格式，base64 编码
  - VLM 调用格式：messages 中使用 image_url 类型，URL 格式为 data:image/jpeg;base64,{img_b64}
  - 返回结果需要提取 JSON（去除可能的 markdown 包裹）
  - 轻量读图结果自动更新到对应的 ImageAsset 对象
---

## 2026-04-11 22:05 - US-003
- 实现了 image_assets.json 的读写功能
- 文件变更：修改 utils/image_assets.py
- **Learnings for future iterations:**
  - save_to_json() 自动创建输出目录
  - JSON 格式：顶层包含 "assets" 数组
  - load_from_json() 支持增量更新：只更新空字段，不覆盖已有数据
  - 增量更新逻辑：如果资产已存在，只填充空字段；如果不存在，直接添加
  - 这种设计允许用户手动编辑 JSON 后重新加载，不会丢失已有的轻量读图结果
---

## 2026-04-11 22:10 - US-004
- 创建了 brief 文件结构和管理器
- 文件变更：创建 utils/brief_manager.py
- **Learnings for future iterations:**
  - Brief 使用 Markdown + YAML frontmatter 格式存储
  - YAML frontmatter 包含结构化数据，Markdown body 包含可读内容
  - 使用正则表达式解析 frontmatter：`^---\s*\n(.*?)\n---\s*\n(.*)$`
  - BriefManager 提供 load/save/update 三个核心方法
  - update() 方法支持部分字段更新，自动保存
  - Brief 数据类包含：goal（必填）、audience、style_preference、constraints（列表）、image_requirements（字典列表）
---

## 2026-04-11 22:15 - US-005
- 创建了 PM Agent 基础框架
- 文件变更：创建 agents/pm.py
- **Learnings for future iterations:**
  - PMAgent 是系统的入口和协调者，负责接收用户输入并路由到正确阶段
  - PMAgent 持有 BriefManager 和 ImageAssetsManager 两个管理器
  - intake() 方法接收用户输入（text/images/urls/template_pptx）
  - determine_gate() 方法根据文件存在情况判断应进入哪个 Gate（Content/Visual/Execute）
  - _extract_images_from_input() 和 _organize_user_intent() 是占位实现，将在后续 US 中完善
  - 项目使用相对导入（from ..utils），需要作为模块运行（python -m tools.nano_banana_ppt.main）
---

## 2026-04-11 22:20 - US-006
- 实现了 PM Agent 的图片抽取逻辑
- 文件变更：修改 agents/pm.py
- **Learnings for future iterations:**
  - 图片抽取分为三个来源：本地路径、URL 下载、PPTX 提取
  - 本地路径抽取：检查文件存在性，避免重复添加，自动执行轻量读图
  - URL 下载：使用 requests 库，生成 hash 文件名避免冲突，保存到 output/images/ 目录
  - PPTX 提取：使用 python-pptx 库，遍历所有 slide 的 shapes，检查 hasattr(shape, "image")
  - PPTX 图片保存格式：pptx_slide{N}_img{M}.{ext}，保留原始扩展名
  - 所有抽取的图片都会自动调用 light_read_image() 进行轻量读图
  - 抽取完成后统一调用 save_to_json() 持久化
  - 错误处理：对每个图片单独 try-catch，一个失败不影响其他图片
---

## 2026-04-11 22:30 - US-007
- 实现了 PM Agent 的用户意图整理逻辑
- 文件变更：修改 agents/pm.py
- **Learnings for future iterations:**
  - 使用 LLM (gemini-2.0-flash-exp) 分析用户输入，提取结构化信息
  - 识别 6 种输入类型：topic/article/outline/template_ppt/add_images/modify_slide
  - 提取字段：goal, audience, tone, constraints, image_anchors
  - Brief 更新策略：如果不存在则创建，如果存在则增量合并（去重）
  - 约束和图片需求采用增量合并，避免覆盖已有数据
  - JSON 解析需要处理 markdown 代码块包裹（去除 ```json 标记）
  - 失败时创建最小化 Brief（使用输入前 200 字符作为 goal）
  - 这个方法是 PM Agent 的核心能力，将自然语言转换为结构化任务定义
---

## 2026-04-11 22:11 - US-008
- 实现了 content_plan.md 到 content_plan.json 的编译器
- 文件变更：创建 utils/compile_content_plan.py
- **Learnings for future iterations:**
  - 使用正则表达式解析 Markdown 标题层级
  - 图片锚点格式：[IMAGE: 语义锚点]
  - 自动推断幻灯片类型（cover/section/content/quote/data）
  - JSON 输出包含 slides 数组和元数据
---


## 2026-04-11 22:35 - US-009
- 升级 NarrativeAgent 输出 content_plan.md
- 文件变更：修改 agents/narrative.py
- **Learnings for future iterations:**
  - 完善了 generate_narrative_outline() 方法，实现三种模式判断逻辑
  - 新增 save_content_plan_md() 方法，生成包含图片语义锚点的 Markdown 格式
  - 图片锚点格式：[IMAGE: 语义锚点]，插入在内容段落后
  - 新增 _assign_image_anchor() 方法，根据页面内容智能匹配 brief 中的图片需求
  - 生成 Markdown 后自动调用 compile_content_plan() 生成 JSON
  - 根据页面类型（cover/section）决定标题层级（# 或 ##）
  - 支持从 BriefManager 读取图片需求（image_requirements）
  - 锚点匹配使用简单的关键词匹配策略
---

## 2026-04-11 23:00 - US-010
- 实现了文档规范化工具
- 文件变更：创建 utils/doc_normalizer.py
- **Learnings for future iterations:**
  - 实现了三个规范化函数：normalize_content_plan(), normalize_visual_plan(), normalize_brief()
  - 规范化策略：自动修复格式问题，检测并报告无法自动修复的问题
  - 常见格式问题包括：标题缺少空格、图片锚点格式不规范、中英文标点混用、多余空行、行尾空格
  - 返回 (normalized_content, issues) 元组，issues 是问题列表
  - 图片锚点格式：[IMAGE: 语义描述]，统一大小写，确保冒号后有空格
  - 检测缺失的必填字段：content_plan 需要标题，visual_plan 需要 path 和合法的 mode，brief 需要 goal
  - 文件规范化不改变语义，只修复格式问题
---

## 2026-04-11 23:30 - US-011
- 实现了 visual_plan.md 的半结构化图片块格式
- 文件变更：修改 utils/doc_normalizer.py
- **Learnings for future iterations:**
  - 定义了 ImageBlock 数据类，包含 path, mode, role, position 字段
  - 图片块格式使用 ```image 代码块，字段以 key: value 形式表示
  - 实现了 parse_image_blocks() 函数，解析图片块并返回 ImageBlock 对象列表
  - 支持一页多图：同一个标题下可以有多个图片块
  - 支持待补图占位：path 可以设置为 "PLACEHOLDER"
  - 图片块必填字段：path 和 mode
  - 图片块可选字段：role（图片作用）和 position（图片位置）
  - mode 必须是三种之一：INTENT_FUSION（意向融合）、ELEMENT_PRESERVE（元素保留）、ORIGINAL_PRESENT（原图呈现）
  - 解析函数会验证字段完整性和合法性，返回详细的错误报告
  - normalize_visual_plan() 升级为调用 parse_image_blocks()，统计一页多图和待补图情况
  - ImageBlock 提供 to_markdown() 方法，可以将对象转换回 Markdown 格式
---

## 2026-04-11 23:45 - US-012
- 升级 VisualAgent 输出 visual_plan.md
- 文件变更：修改 agents/visual.py
- **Learnings for future iterations:**
  - 新增 save_visual_plan_md() 方法，生成半结构化图片块格式
  - 图片块格式使用 ```image 代码块，包含 path, mode, role, position 字段
  - mode 必须使用大写格式（INTENT_FUSION/ELEMENT_PRESERVE/ORIGINAL_PRESENT）
  - 根据 image_assets.json 中的语义锚点（semantic_anchor）分配图片到页面
  - 推荐模式优先级：用户指定 mode > 轻量读图推荐 mode > 默认 INTENT_FUSION
  - 新增 _recommend_position() 方法，根据图片类型和页面类型推荐位置
  - 支持待补图占位：path 设置为 "PLACEHOLDER"
  - 封面和章节页默认添加占位图（如果没有图片锚点）
  - 图片位置推荐逻辑：封面/章节/hero 使用 full，截图/产品/图表使用 center，人物使用 right，场景使用 full
---

## 2026-04-11 23:55 - US-013
- 实现了 visual_plan.md 到 visual_plan.json 的编译器
- 文件变更：创建 utils/compile_visual_plan.py
- **Learnings for future iterations:**
  - 编译器解析 visual_plan.md 中的图片块，生成可执行的 JSON 格式
  - 根据三种图片模式生成不同的 visual_prompt 策略：
    * INTENT_FUSION（意向融合）：只取语义，完全重新创作
    * ELEMENT_PRESERVE（元素保留）：保留主体元素，允许重组背景和布局
    * ORIGINAL_PRESENT（原图呈现）：保留长宽比，只做轻微加工
  - JSON 输出包含 pages 数组，每个 page 包含 page_number, title, images
  - 每个 image 包含 path, mode, role, position, visual_prompt 字段
  - visual_prompt 是根据模式自动生成的完整提示词，包含页面上下文和创作要求
  - 支持 PLACEHOLDER 占位符，编译时会统计待补图数量
  - 使用 parse_image_blocks() 复用 doc_normalizer 的解析逻辑
  - 编译器可以作为模块导入或命令行工具使用
---

## 2026-04-11 23:59 - US-014
- 实现了资源完整性检查工具
- 文件变更：创建 utils/resource_check.py
- **Learnings for future iterations:**
  - 定义了 CheckIssue 和 ResourceCheckReport 数据类，结构化表示检查结果
  - 检查分为四个维度：图片路径、待补图占位符、必填字段、文档一致性
  - 问题分为三个严重级别：error（阻塞执行）、warning（需要注意）、info（提示信息）
  - 问题分类：missing_file（文件缺失）、placeholder（待补图）、missing_field（字段缺失）、inconsistency（不一致）
  - 检查逻辑支持开关控制（check_images/check_placeholders/check_fields/check_consistency）
  - 图片路径检查支持相对路径和绝对路径
  - 一致性检查包括：页数一致性、标题一致性
  - 返回详细的检查报告，包含问题列表和统计摘要
  - 提供 print_report() 函数，友好地打印检查结果
  - 可以作为模块导入或命令行工具使用
  - 只有 error 级别的问题会导致 passed=False
---

## 2026-04-11 22:31 - US-015
- 将 Reviewer 降级为工具函数
- 文件变更：创建 utils/prompt_guard.py
- **Learnings for future iterations:**
  - 实现了分层审查策略：全量轻检查（快速扫描）+ 种子页深查 + 异常页必查
  - 定义了 CheckIssue 和 ReviewReport 数据类，结构化表示审查结果
  - 轻量检查不调用 LLM，快速扫描明显问题（PLACEHOLDER、空 prompt、非法 mode、长度异常）
  - 深度检查调用 LLM 审阅，从 6 个维度改进 prompt（风格、内容、技术、叙事、精简、情感）
  - 审查策略支持 4 种模式：auto（自动）、light（仅轻检查）、deep（全量深查）、seed_only（仅种子页）
  - 返回 ReviewReport 包含问题列表（issues）和改进建议（patches）
  - 问题分为 3 个严重级别：error（阻塞）、warning（需注意）、info（提示）
  - 问题分为 6 个类别：style、content、technical、narrative、redundancy、emotion
  - 从 ReviewerAgent 迁移的核心逻辑：_review_single_prompt() 调用 LLM 审阅单个 prompt
  - 使用 gemini-2.0-flash-exp 快速模型进行审阅，temperature=0.3
  - 计算 prompt 差异比例（diff_ratio），超过 30% 差异时记录为 info 级别问题
  - 提供 print_review_report() 函数，友好地打印审查报告
---

## 2026-04-11 23:59 - US-016
- 实现了 PM Agent 的最终意图审查功能
- 文件变更：修改 agents/pm.py
- **Learnings for future iterations:**
  - 实现了 final_intent_review() 方法，整合三层审查：资源完整性、Prompt 质量、Brief 一致性
  - 资源完整性检查：调用 resource_check.check_resources()，检查文件存在性、图片路径、待补图、必填字段
  - Prompt 质量审查：调用 prompt_guard.review_visual_plan()，使用 auto 策略（轻检查 + 种子页深查 + 异常页必查）
  - Brief 一致性检查：实现 _check_brief_visual_consistency() 方法，对比 Brief 和 visual_plan 的图片需求和风格偏好
  - 审查报告结构：包含 passed（是否通过）、resource_check、prompt_review、consistency_check、summary（统计信息）
  - 问题分级：blocking_issues（阻塞问题，error 级别）、warnings（警告）、total_issues（总问题数）
  - 只有 blocking_issues == 0 时才通过审查
  - 审查报告会打印详细的统计信息和问题列表
  - 一致性检查包括：Brief 中的图片需求是否在 visual_plan 中体现、风格偏好是否在 prompt 中体现
  - 使用 logger 输出审查过程和结果，方便调试和追踪
---

## 2026-04-11 23:59 - US-017
- 升级 executor 支持新的 visual_plan.json 格式
- 文件变更：修改 core/executor.py
- **Learnings for future iterations:**
  - 实现了 _convert_new_format_to_slides() 函数，将新格式（pages 数组）转换为旧格式（slides 数组）
  - 新格式包含 pages 数组，每个 page 有 page_number, title, images 字段
  - 每个 image 包含 path, mode, role, position, visual_prompt 字段
  - 实现了三种图片模式的处理策略：
    * INTENT_FUSION（意向融合）：不设置 blend_reserved_region，允许完全重绘
    * ELEMENT_PRESERVE（元素保留）：设置中等保留区域（0.2-0.8），保留主体元素
    * ORIGINAL_PRESENT（原图呈现）：设置全图保留区域（0.05-0.95），最小化改动
  - 实现了 _infer_page_type() 函数，根据页码和标题推断页面类型（cover/section/hero/content）
  - 实现了 _build_native_image_entry() 函数，根据图片模式构建 native_image 条目
  - 实现了 _position_to_bounding_box() 函数，将位置描述转换为 bounding_box
  - 支持一页多图：每个 page 的 images 数组可以包含多张图片，自动转换为 native_images
  - 保持向后兼容：如果 JSON 不包含 pages 字段，使用旧格式逻辑
  - 所有图片统一使用 blend 模式（已废弃 overlay）
  - visual_prompt 从 compile_visual_plan 生成，包含完整的创作要求和上下文
---

## 2026-04-11 23:59 - US-018
- 升级 generator 支持三种图片模式
- 文件变更：修改 core/generator.py
- **Learnings for future iterations:**
  - 实现了三种图片模式的 prompt 策略：
    * INTENT_FUSION（意向融合）：只取语义和概念，完全重新创作，不保留可识别元素
    * ELEMENT_PRESERVE（元素保留）：保留主体元素（人物/产品/关键对象），但可以重组背景和次要元素
    * ORIGINAL_PRESENT（原图呈现）：像素级保留原图，只允许轻微的光影调整
  - 图片模式从 native_images 的 mode 字段读取
  - 只有 integration_mode='blend' 的图片才应用模式策略
  - overlay 模式的图片不受影响（它们是后期叠加的）
  - 每种模式都有明确的 prompt 指令，告诉 AI 如何处理参考图片
  - ORIGINAL_PRESENT 最严格（保留一切），INTENT_FUSION 最自由（只取灵感）
  - 这个实现与 US-017 中 executor 设置的 blend_reserved_region 配合工作
---

## 2026-04-11 - US-019: 移除 overlay 相关代码

**目标**: 统一使用 blend 模式，移除所有 overlay 相关代码

**实现**:
- 移除 utils/bbox.py 中的 _lock_overlay_bbox 函数
- 移除 core/generator.py 中的 _merge_native_images_with_locked_regions 函数和 overlay 相关逻辑
- 移除 core/image_selector.py 中的 overlay 评分字段和相关 prompt
- 更新 utils/review_plan.py 将所有 overlay 默认值改为 blend，兼容旧格式
- 删除 tests/test_mixed_layout_locking.py 测试文件
- 更新 tests/test_image_selector_logic.py 移除 overlay 评分字段

**测试**: 所有测试通过 (5 passed)

**提交**: c22d735 - feat: US-019 - 移除 overlay 相关代码

## 2026-04-11 - US-020: 实现局部修改的回退判断逻辑

**目标**: 实现 PMAgent.analyze_modification() 方法，用于分析用户修改请求并判断回退策略

**实现**:
- 实现 analyze_modification() 方法，接收修改请求和目标页面参数
- 使用 LLM 分析修改类型（content/visual/structure/image_assignment）
- 智能判断应回退到哪个 Gate（Content/Visual/Execute）
- 判断是否可以只修改单页，避免不必要的全局重建
- 实现 _get_all_page_numbers() 辅助方法从 content_plan.json 或 visual_plan.json 获取页面编号
- 返回详细的分析结果，包含修改类型、回退 Gate、受影响页面、判断理由和操作建议

**测试**: 所有测试通过 (5 passed)

**提交**: 481e844 - feat: US-020 - 实现局部修改的回退判断逻辑

[2025-01-XX] US-021 完成
- 创建 utils/plan_sync.py 模块
- 实现 sync_content_plan() 和 sync_visual_plan() 函数
- 支持 JSON -> Markdown 的双向同步
- 自动检测变更并记录修改历史
- Commit: fc65a7c

## 2026-04-11 23:59 - US-022
- 实现了执行失败的分类处理
- 文件变更：创建 core/failure_classifier.py，修改 core/executor.py
- **Learnings for future iterations:**
  - 定义了 FailureType 枚举：MATERIAL（素材）、GENERATION（生成）、PLAN（计划）、ASSEMBLY（组装）
  - 定义了 FailureSeverity 枚举：TRANSIENT（暂时性，可重试）、PERMANENT（永久性，需人工介入）
  - 实现了 classify_failure() 函数，根据异常类型和错误信息自动分类失败
  - 实现了 FailureReport 数据类，包含失败类型、严重程度、页码、文件路径、阶段、错误信息、重试次数等
  - 素材问题检测：FileNotFoundError、文件不存在、权限错误等，标记为永久性失败
  - 生成问题检测：API 错误、超时、网络问题等，区分暂时性和永久性失败
  - 计划问题检测：JSON 解析错误、字段缺失等，标记为永久性失败
  - 组装问题检测：PPTX 创建失败、模板问题等，标记为永久性失败
  - 暂时性错误自动重试，最多 3 次，每次等待时间递增（5s、10s、15s）
  - 永久性错误不重试，直接报告用户
  - 种子页生成失败会进行智能重试，失败后使用灰色占位图
  - 并行生成阶段收集所有失败报告，最后统一打印摘要
  - 实现了 generate_failure_summary() 函数，生成详细的失败摘要报告
  - 失败摘要按类型分组，统计可重试和需人工处理的失败数量
  - 组装阶段失败会被捕获并分类，返回 None 而不是抛出异常
---

## 2026-04-11 23:59 - US-023
- 升级 main.py 集成 PM Agent
- 文件变更：修改 main.py
- **Learnings for future iterations:**
  - 在 generate_content_plan() 中初始化 PMAgent，调用 intake() 接收用户输入
  - PM Agent 负责维护 brief.md 和 image_assets.json
  - 在 generate_visual_plan() 中调用 determine_gate() 判断当前应该在哪个阶段
  - 在 execute_from_plan() 中调用 final_intent_review() 执行最终审查
  - 最终审查包含三层检查：资源完整性、Prompt 质量、Brief 一致性
  - 使用 --force 参数可以跳过最终审查（用于调试或紧急情况）
  - PM Agent 需要 OpenAI 兼容的 client，使用 openai.OpenAI 初始化
  - 保留了所有现有的命令行接口，PM Agent 是透明集成的
  - PM Agent 的 intake() 方法接收结构化输入：text, images, urls, template_pptx
  - briefing 参数会传递给 PM Agent 作为用户意图文本
---
